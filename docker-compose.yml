services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_DB: noticias_db
      POSTGRES_USER: noticias_user
      POSTGRES_PASSWORD: noticias_pass
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
